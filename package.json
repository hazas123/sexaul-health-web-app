{
  "name": "sexual-health-web-app",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "lint": "standard ./",
    "precommit-msg": "echo 'Pre-commit checks...' && exit 0"
  },
  "pre-commit": [
    "precommit-msg",
    "lint"
  ],
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "jquery": "^3.2.1",
    "meteor-node-stubs": "^0.3.2"
  },
  "devDependencies": {
    "standard": "^10.0.3"
  },
  "standard": {
    "globals": [ "$" ]
  }

}
